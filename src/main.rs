// MOD  is the module/ file that can be bought in for use
//mod print;  //calls the println file into main 
//mod vars;
//mod types; 
//mod strings;
//mod tuples;
//mod arrays;
//mod vectors;
//mod conditionals;
//mod loops;
//mod functions;
//mod pointers;
//mod structs;
//mod enums; 
mod cli; 

fn main() {
    //print::run(); //runs (a fn) the print file 
    //vars::run(); 
    //types::run();
    //strings::run();
    //tuples::run();
    //arrays::run(); // in Arrays file, do the Run FN
    //vectors::run();
    //conditionals::run();
    //loops::run();
    //functions::run();
    //pointers::run(); 
    //structs::run(); 
    //enums::run(); 
    cli::run();
}
